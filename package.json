{
  "name": "pkgs",
  "private": true,
  "version": "1.0.0",
  "description": "一些包",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "lerna run dev --ignore=docs",
    "build": "lerna run build --ignore=docs",
    "release:alpha": "npm run release -- --canary --force-publish",
    "release:beta": "release:alpha -- --preid beta",
    "release": "lerna publish --no-private --registry=https://registry.npmjs.org/",
    "test": "lerna run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cloudyan/pkgs.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cloudyan/pkgs/issues"
  },
  "homepage": "https://github.com/cloudyan/pkgs#readme",
  "devDependencies": {
    "lerna": "^6.6.1",
    "nx": "^15.9.2"
  }
}
